# RPG Stage Web Client

一个基于 React 和 TypeScript 构建的 RPG Stage API 客户端，提供清晰的架构和美观的用户界面。

## 功能特性

- 🔐 用户登录认证
- 🤖 AI 代理管理
- 💬 实时对话界面
- 🎨 美观的现代化 UI
- ⚠️ 完善的错误处理
  - 消息发送错误：独立窗口显示
  - 其他错误：弹窗提示

## 技术栈

- **React 19** - UI 框架
- **TypeScript** - 类型安全
- **Vite** - 构建工具
- **React Router** - 路由管理
- **Axios** - HTTP 客户端

## 项目结构

```
src/
├── components/          # UI 组件
│   ├── common/         # 通用组件（布局、错误弹窗等）
│   ├── auth/           # 认证相关组件
│   ├── agents/         # 代理相关组件
│   └── conversations/  # 对话相关组件
├── contexts/           # React Context（认证等）
├── pages/              # 页面组件
├── services/           # API 服务
├── types/              # TypeScript 类型定义
└── utils/              # 工具函数和常量
```

## 快速开始

### 安装依赖

```bash
npm install
```

### 配置环境变量

复制 `.env.example` 为 `.env` 并配置 API 地址：

```bash
cp .env.example .env
```

编辑 `.env` 文件：

```
VITE_API_BASE_URL=http://localhost:3000
```

### 启动开发服务器

```bash
npm run dev
```

访问 http://localhost:5173

### 构建生产版本

```bash
npm run build
```

### 预览生产构建

```bash
npm run preview
```

## API 接口

客户端实现了以下 API 接口：

- **认证**
  - POST `/auth/session` - 登录
  - DELETE `/auth/session` - 登出

- **代理管理**
  - GET `/agents` - 获取代理列表
  - GET `/agents/:id` - 获取代理详情
  - POST `/agents` - 创建代理
  - POST `/agent_metas` - 创建代理元数据

- **对话管理**
  - GET `/agents/:id/conversations` - 获取对话列表
  - POST `/agents/:id/conversations` - 创建新对话
  
- **消息管理**
  - GET `/conversations/:id/messages` - 获取消息历史
  - POST `/conversations/:id/messages` - 发送消息

## 错误处理

### 消息发送错误
当发送消息到服务器时发生错误，系统会在一个独立的错误窗口中显示错误信息，该窗口具有醒目的红色边框和专门的样式。

### 其他错误
对于其他操作（如加载数据、创建资源等）产生的错误，系统会使用模态弹窗显示错误信息。

## 开发说明

### 代码风格

项目使用 ESLint 进行代码检查：

```bash
npm run lint
```

### 添加新功能

1. 在 `src/types` 中定义 TypeScript 类型
2. 在 `src/services` 中添加 API 方法
3. 在 `src/pages` 或 `src/components` 中创建 UI 组件
4. 在 `src/App.tsx` 中添加路由（如需要）

## License

MIT
